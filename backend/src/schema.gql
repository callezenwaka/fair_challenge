# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Email {
  id: Int!
  address: String!
  tokens: [Token!]
}

type Token {
  id: Int!
  name: String!
  launch: DateTime
  emails: [Email!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Query {
  getTokens: [Token!]!
  getToken(id: Int!): Token!
  getEmails: [Email!]!
  getEmail(id: Int!): Email!
}

type Mutation {
  postToken(postTokenInput: postTokenInput!): Token!
  updateToken(updateTokenInput: updateTokenInput!): Token!
  deleteToken(id: Int!): Token!
  subscribeToken(subscribeTokenInput: subscribeTokenInput!): Token!
  postEmail(postEmailInput: postEmailInput!): Email!
  updateEmail(updateEmailInput: UpdateEmailInput!): Email!
  removeEmail(id: Int!): Email!
}

input postTokenInput {
  name: String!
  launch: DateTime
}

input updateTokenInput {
  id: Int!
  name: String!
  launch: DateTime
}

input subscribeTokenInput {
  id: Int!
  address: String!
}

input postEmailInput {
  address: String!
}

input UpdateEmailInput {
  address: String
  id: Int!
}